import {StyleSheet} from 'react-native';
import { Color_Pallete } from './Constants';

export const styles = StyleSheet.create({
  listContainer: {backgroundColor: Color_Pallete.metal_black, height: '100%'},
});